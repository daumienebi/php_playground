# My PHP Playground
Learning / Tinkering with php
These are just my messy notes of anything i feel like jotting down.

To get started, visit https://www.php.net/

# Running locally

* To run a php file with the internal php server, use :
`php -S localhost:8000 <fileName>.php`

* To run the server under a whole folder, just navigate to the server directory using the command line and type in:
`php -S localhost:8000`

    This can be useful when you need more than one file to be served, in a form for example. (html form and php validating it).
